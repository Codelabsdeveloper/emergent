[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — fixes 404 on /admin, /admin/dashboard, refresh, deep links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
